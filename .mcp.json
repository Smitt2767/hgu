{
  "mcpServers": {
    "payload-hgu": {
      "type": "http",
      "url": "https://localhost:3000/api/mcp",
      "headers": {
        "Authorization": "Bearer ${PAYLOAD_MCP_API_KEY}"
      }
    }
  }
}
