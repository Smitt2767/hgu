{
  "mcpServers": {
    "payload-hgu": {
      "type": "http",
      "url": "${PAYLOAD_MCP_URL:-http://localhost:3000/api/mcp}",
      "headers": {
        "Authorization": "Bearer ${PAYLOAD_MCP_API_KEY}"
      }
    }
  }
}
